# Test configuration
spring.application.name=cafe-finder-test
server.port=0
spring.data.mongodb.uri=mongodb://localhost:27017/cafe_finder_test

# JWT Configuration for tests
cafefinder.app.jwtSecret=testsecretkeyforjwt
cafefinder.app.jwtExpirationMs=604800000

# CORS Configuration for tests
cors.allowed-origins=http://localhost:5173

# Google Places API Configuration for tests (empty to avoid API calls)
google.places.api.key=
google.places.api.base.url=https://maps.googleapis.com/maps/api/place

